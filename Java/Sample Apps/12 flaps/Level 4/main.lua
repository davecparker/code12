this = {}; _fn = {}   -- This file was generated by Code12 from "level4.java"
package.path = package.path .. ';../../Desktop/Code12/?.lua;../../../Desktop/Code12/?.lua'
require('Code12.api')
    
    
        
        
    
    
    function _fn.start()
        
        --draws the background
        ct.setBackImage("background-day.png")
        
        --sets the height of the sceen to match the height of the background
        local h = 128
        ct.setHeight(h)
        local w = 100
        
        --draws the bird ct.image("filename", x, y, h)
        local x = w / 2.0
        local y = h / 2.0
        local hbird = 10
        ct.image("yellowbird-downflap.png", x, y, hbird)
        
        --draws the count
        --the value of x does not change (the x position of the bird and number are equal)
        y = h / 8.0
        local hnumb = 5
        ct.image("0.png", x, y, hnumb)
        
        --draws the ground
        y = 122
        h = 40
        x = 20
        ct.image("base.png", x, y, h)
        x = x + 40
        ct.image("base.png", x, y, h)
        x = x + 20
        ct.image("base.png", x, y, h)
    end
    
    function _fn.update()
        
        local filename = nil; 
    end
