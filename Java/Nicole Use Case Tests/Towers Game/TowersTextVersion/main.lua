this = {}; _fn = {}   -- This file was generated by Code12 from "TowersTextVersion.java"
package.path = package.path .. ';../../Desktop/Code12/?.lua;../../../Desktop/Code12/?.lua'
require('Code12.api')






    
    this.totalDisks = 0; 
    this.count = 0
    
        
        
    
    
    function _fn.start()
        
        this.totalDisks = ct.inputInt("Enter total number of disks: ")
    end
    
    function _fn.update()
        
        _fn.solveTowers(this.totalDisks, "A", "B", "C")
    end
    
    function _fn.solveTowers(top, from, inter, to)
        
        if top == 1 then
            ct.println("Disk 1 from " .. from .. " to " .. to); 
        else 
            
            _fn.solveTowers(top - 1, from, to, inter)
            ct.println("Disk " .. top .. " from " .. from .. " to " .. to)
            _fn.solveTowers(top - 1, inter, from, to)
        end
    end
    
