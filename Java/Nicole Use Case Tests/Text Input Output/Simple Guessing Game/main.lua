this = {}; _fn = {}   -- This file was generated by Code12 from "SimpleGuessingGame.java"
package.path = package.path .. ';../../Desktop/Code12/?.lua;../../../Desktop/Code12/?.lua'
require('Code12.api')

    
    
    this.guess = 0
    this.randomNumber = 0; 
    this.numOfGuesses = 0
    
    
        
        
    
    
    function _fn.start()
        
        this.randomNumber = ct.random(1, 100)
        local stopped = false
        
        ct.println("******************************")
        ct.println("Welcome to the guessing game!")
        ct.println("******************************")
        ct.println( "" )
        
        while not stopped do
            
            this.guess = ct.inputInt("Enter a number between 1 and 100 to begin: ")
            if this.guess < 1 or this.guess > 100 then
                this.guess = ct.inputInt("A number between 1 and 100, please: "); 
            else 
                
                this.numOfGuesses = this.numOfGuesses + 1
                
                while this.guess ~= this.randomNumber do
                    
                    if this.guess < this.randomNumber then
                        ct.print("Guess higher! "); 
                    elseif this.guess > this.randomNumber then
                        ct.print("Guess lower! "); end
                    
                    ct.println("Try again... ")
                    this.guess = ct.inputInt("Enter a number from 1 - 100: ")
                    this.numOfGuesses = this.numOfGuesses + 1
                end
                
                if this.guess == this.randomNumber then
                    ct.println("You win after "..tostring(this.numOfGuesses).." guesses!"); end
                
                local decide = ct.inputYesNo("Want to play again? Enter y/n: ")
                if decide == false then
                    stopped = true; end
            end
            
        end
        
        ct.println("Thanks for playing!")
    end
    
