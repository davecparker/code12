import Code12.*;

class UserCode extends Code12Program
{
	boolean x =  -(10 + -20) * (obj.x + 40) > 0 && !!foo(x, a + b);

	public void start()
	{
		// Draw some circles
		ct.circle(250, 100, 50);
		if (x)
			ct.circle(100, 200, 150);
		ct.circle(200, 400, 200);
	}
}
