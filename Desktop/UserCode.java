import Code12.*;

class UserCode extends Code12Program
{
	public void start()
	{
		// Test parse line 8
		boolean x =  -(10 + -a[i]) * (obj.x + 40) > 0 && !!foo(x, a + b);

		// Draw some circles
		ct.circle(250, 100, 50);
		ct.circle(100, 200, 150);
		ct.circle(200, 400, 200);
	}
}
